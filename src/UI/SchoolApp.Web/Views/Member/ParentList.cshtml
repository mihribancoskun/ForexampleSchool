@using SchoolApp.Modul.School.Provider
@model List<ParentViewModel>
@{
    Layout = null;
}

<div class="modal-header">
    <h5 class="modal-title" id="exampleModalLabel">Ebeveyn Listesi</h5>
    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
        <span aria-hidden="true">&times;</span>
    </button>
</div>
<div class="modal-body">
    @*<input type="hidden" name="Id" value="@Model.Id" />*@

    <div class="kt-portlet__body">
        <div class="kt-section kt-section--first">
            @*<div class="form-group">
                    <label>İsim:</label>
                    <input type="text" class="form-control" name="Name" value="@Model.Name">
                </div>
                <div class="form-group">
                    <label>Soyisim:</label>
                    <input type="text" class="form-control" name="Surname" value="@Model.Surname">
                </div>
                <div class="form-group">
                    <label>Okul No:</label>
                    <input type="number" class="form-control" name="SchoolNo" value="@Model.SchoolNo">
                </div>*@



        </div>

        <table class="table">
            <thead>
                <tr>
                    <th scope="col">İsim</th>
                    <th scope="col">Soyisim</th>
                    <th scope="col">App Şiffre</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var p in Model)
                {
                    <tr>
                        <td> @p.Name </td>
                        <td>@p.Surname</td>
                        <td>--</td>
                    </tr>
                }

            </tbody>
        </table>




    </div>



</div>


<div class="modal-footer">
    <button type="button" class="btn btn-secondary" data-dismiss="modal">Kapat</button>
</div>


<script>
    function memberUpdate() {
        var _formSeriliaz = $("#formmember").serialize();
        $.ajax({
            url: "/member/update",
            type: 'POST',
            data: _formSeriliaz,
            success: function (data) {
                alert(data);
            }
        });
    }
</script>
